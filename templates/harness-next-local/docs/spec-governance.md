# Spec Governance

Significant work follows:

```text
spec -> plan -> tasks -> acceptance -> implementation -> verification -> handoff
```

## Rules

- Write the acceptance command before implementation.
- Keep specs small enough for a Worker to finish and verify.
- If a requirement is ambiguous, update the spec instead of relying on chat memory.
- Cross-layer changes require Orchestrator review.

